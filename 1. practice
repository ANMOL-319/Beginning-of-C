#include <stdio.h>
int main(){
    int length , breadth;

    
    printf("Enter length :");
    scanf("%d", &length);

    printf("Enter Breadth :");
    scanf("%d", &breadth);
    int area = length*breadth;

    printf("The area of rectangle is %d", area);

    return 0;
    }
