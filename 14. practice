#include <stdio.h>

struct vector{
    int x;
    int y;

};
int main(){
    struct vector v1, v3;
    v1.x = 34;
    v1.y = 54;

printf("X dim is %d and Y dim is %d\n", v1.x, v1.y);

    v3.x = 3345;
    v3.y = 563;

printf("X dim is %d and Y dim is %d\n", v3.x, v3.y);

    return 0;}
