#include <stdio.h>
int main()
{
    int radius = 3;
    float pi = 3.14;

    printf("The area of circle is %f\n", pi * radius * radius);
    int height = 10;

    printf("The volume of cylinder is %f\n", 2 * pi * radius * height);

    return 0;
}
