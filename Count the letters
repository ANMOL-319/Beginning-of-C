#include <stdio.h>
#include <string.h>


int main(){
    char *st = "Anmol Shrestha";
    int a = strlen(st); // strlen() = It will count the letters excluding null('\0) value. 
    printf("The length of the string is %d", a);

    
    return 0;}
